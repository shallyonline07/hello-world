import { KeyMouseEventsDirective } from './key-mouse-events.directive';

describe('KeyMouseEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyMouseEventsDirective();
    expect(directive).toBeTruthy();
  });
});
